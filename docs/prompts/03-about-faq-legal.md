# Phase 3 — About, FAQ, Legal

**`(public)/about`** — the trust page. Team and offices using `/public/team`
and `/public/office` images. Include what the firm *does not* do (no PR, study,
work or sponsorship) — stating limits builds more trust than claiming range.

**`(public)/faq`** — accordion. Scannable and quiet. No decoration.

**`(public)/legal/[slug]`** (349 lines) — use the `Prose` primitive in
`ui/section.tsx`. Long-form, generous measure, no ornament.

Report what you verified vs assumed. Then stop.
