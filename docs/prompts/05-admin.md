# Phase 5 — Admin

`src/app/admin/(dashboard)/` — 15 screens. Clean CRM, not a marketing surface.

- **Reduce decorative KPI cards.** Density is a feature here; a case manager
  works a queue.
- Prioritise tables, status, filters, timelines, actionable drawers.
- **Applications needs Kanban + List views.**
- Tables must scroll horizontally on mobile; the sidebar already has a
  `lg:hidden` drawer.

Do overview → applications → leads → documents first; the rest after.
One screen at a time, typecheck between each. Then stop.
