import Link from "next/link";
import Image from "next/image";
import {
  ArrowUpRight,
  ShieldCheck,
  Sparkles,
  Compass,
  Globe2,
  CheckCircle2,
  Quote,
  Plane,
  Users,
  Star,
  Award,
  Zap,
  Clock
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Stat } from "@/components/ui/stat";
import { Reveal, RevealStagger, RevealItem } from "@/components/motion/reveal";
import {
  DESTINATIONS,
  SERVICES,
  ORIGIN_HUBS,
  servicesForDestination,
  type Service
} from "@/lib/constants";
import { DEMO_UPDATES, DEMO_POSTS, DEMO_STORIES } from "@/lib/content";
import { cn } from "@/lib/utils";

export default function HomePage() {
  return (
    <>
      <Hero />
      <TrustBand />
      <DestinationsDiscovery />
      <VisaServices />
      <EligibilityBlock />
      <WhyTrustGate />
      <ProcessTimeline />
      <SuccessStories />
      <UpdatesPreview />
      <BlogPreview />
      <FaqPreview />
      <ConsultationCta />
    </>
  );
}
      <BlogPreview />
      <FaqPreview />
      <ConsultationCta />
    </>
  );
}

/* ─────────────────────────── Hero ─────────────────────────── */

function Hero() {
  return (
    <section className="relative isolate overflow-hidden border-b border-border/40">
      <HeroBackdrop />
      <div className="container-edge relative pb-24 pt-16 lg:pb-32 lg:pt-24">
        <div className="grid items-end gap-12 lg:grid-cols-12">
          <div className="lg:col-span-7">
            <Reveal>
              <Badge
                variant="gold"
                className="px-3 py-1 text-[11px] uppercase tracking-[0.18em]"
              >
                <Sparkles className="h-3 w-3" />
                Visitor &amp; tourist visas · UK and India
              </Badge>
            </Reveal>
            <Reveal delay={0.05}>
              <h1 className="mt-6 text-balance font-display text-[2.6rem] font-semibold leading-[1.05] tracking-tight sm:text-6xl lg:text-[5.25rem]">
                The trip is yours.
                <br className="hidden sm:block" /> The{" "}
                <span className="relative inline-block">
                  <span className="relative z-10 bg-gradient-to-br from-amber-300 via-amber-400 to-amber-600 bg-clip-text text-transparent">
                    paperwork
                  </span>
                  <span className="absolute -bottom-1 left-0 right-0 -z-0 h-3 bg-amber-300/30 blur-xl" />
                </span>{" "}
                is ours.
              </h1>
            </Reveal>
            <Reveal delay={0.1}>
              <p className="mt-6 max-w-xl text-pretty text-base leading-relaxed text-muted-foreground sm:text-lg">
                A specialist visitor and tourist visa consultancy for applicants
                in the United Kingdom and India. Schengen, Australia, New
                Zealand, Canada, the USA, Turkey and worldwide - plus UK visa
                extensions.
              </p>
            </Reveal>
            <Reveal delay={0.15}>
              <div className="mt-8 flex flex-wrap items-center gap-3">
                <Button asChild size="xl" variant="gold" className="shadow-lg shadow-amber-500/20">
                  <Link href="/eligibility">
                    Check My Eligibility
                    <ArrowUpRight className="h-4 w-4" />
                  </Link>
                </Button>
                <Button asChild size="xl" variant="outline">
                  <Link href="/contact">Book a Consultation</Link>
                </Button>
              </div>
            </Reveal>
          </div>
        </div>
      </div>
    </section>
  );
}