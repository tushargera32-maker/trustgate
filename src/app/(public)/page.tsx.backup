import Link from "next/link";
import Image from "next/image";
import {
  ArrowUpRight,
  ShieldCheck,
  Sparkles,
  Compass,
  Globe2,
  CheckCircle2,
  Quote,
  Plane,
  Users,
  Star,
  Award,
  Zap,
  Clock
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Stat } from "@/components/ui/stat";
import { Reveal, RevealStagger, RevealItem } from "@/components/motion/reveal";
import {
  DESTINATIONS,
  SERVICES,
  ORIGIN_HUBS,
  servicesForDestination,
  type Service
} from "@/lib/constants";
import { DEMO_UPDATES, DEMO_POSTS, DEMO_STORIES } from "@/lib/content";
import { cn } from "@/lib/utils";

export default function HomePage() {
  return (
    <>
      <Hero />
      <TrustBand />
      <DestinationsDiscovery />
      <VisaServices />
      <EligibilityBlock />
      <WhyTrustGate />
      <ProcessTimeline />
      <SuccessStories />
      <UpdatesPreview />
      <BlogPreview />
      <FaqPreview />
      <ConsultationCta />
    </>
  );
}

function Hero() {
  return (
    <section className="relative isolate overflow-hidden border-b border-border/40">
      <HeroBackdrop />
      <div className="container-edge relative pb-24 pt-16 lg:pb-32 lg:pt-24">
        <div className="grid items-end gap-12 lg:grid-cols-12">
          <div className="lg:col-span-7">
            <Reveal>
              <Badge
                variant="gold"
                className="px-3 py-1 text-[11px] uppercase tracking-[0.18em]"
              >
                <Sparkles className="h-3 w-3" />
                Visitor and tourist visas
              </Badge>
            </Reveal>
            <Reveal delay={0.05}>
              <h1 className="mt-6 text-balance font-display text-5xl font-semibold leading-tight tracking-tight sm:text-6xl lg:text-7xl">
                The trip is yours. The paperwork is ours.
              </h1>
            </Reveal>
            <Reveal delay={0.1}>
              <p className="mt-6 max-w-xl text-pretty text-base leading-relaxed text-muted-foreground sm:text-lg">
                Specialist visa consultancy for UK and India. Expert support for Schengen, Australia, New Zealand, Canada, USA, Turkey and worldwide destinations.
              </p>
            </Reveal>
            <Reveal delay={0.15}>
              <div className="mt-8 flex flex-wrap items-center gap-3">
                <Button asChild size="xl" variant="gold">
                  <Link href="/eligibility">
                    Check Eligibility
                    <ArrowUpRight className="h-4 w-4" />
                  </Link>
                </Button>
                <Button asChild size="xl" variant="outline">
                  <Link href="/contact">Book Consultation</Link>
                </Button>
              </div>
            </Reveal>
          </div>
        </div>
      </div>
    </section>
  );
}

function HeroBackdrop() {
  return (
    <div className="absolute inset-0 -z-10">
      <Image
        src="/hero/hero-background.png"
        alt=""
        fill
        priority
        quality={75}
        className="object-cover object-center opacity-[0.03]"
      />
      <div className="absolute inset-0 bg-gradient-to-b from-background via-background/80 to-background" />
    </div>
  );
}

function TrustBand() {
  return (
    <section className="border-b border-border/40 bg-muted/30 py-12">
      <div className="container-edge">
        <Reveal>
          <div className="grid gap-8 sm:grid-cols-3">
            <Stat value="98%" label="Success Rate" description="Approved applications" />
            <Stat value="2,500+" label="Applications" description="Processed annually" />
            <Stat value="15+" label="Years" description="Experience" />
          </div>
        </Reveal>
      </div>
    </section>
  );
}

function DestinationsDiscovery() {
  return (
    <section className="container-edge my-24">
      <Reveal>
        <div className="text-center">
          <h2 className="font-display text-3xl font-semibold sm:text-4xl lg:text-5xl">Popular Destinations</h2>
        </div>
      </Reveal>
      <div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {DESTINATIONS.slice(0, 6).map((dest) => (
          <Link key={dest.code} href={`/countries/${dest.code}`} className="group rounded-2xl border p-4">
            <div className="text-3xl">{dest.flag}</div>
            <h3 className="mt-2 font-semibold">{dest.name}</h3>
          </Link>
        ))}
      </div>
    </section>
  );
}

function VisaServices() {
  return (
    <section className="border-y bg-muted/20 py-24">
      <div className="container-edge">
        <h2 className="text-center font-display text-3xl font-semibold">Our Services</h2>
      </div>
    </section>
  );
}

function EligibilityBlock() {
  return (
    <section className="container-edge my-24">
      <div className="rounded-3xl border p-12">
        <h2 className="font-display text-3xl font-semibold">Check Eligibility</h2>
      </div>
    </section>
  );
}

function WhyTrustGate() {
  return (
    <section className="container-edge my-24">
      <h2 className="text-center font-display text-3xl font-semibold">Why Choose Us</h2>
    </section>
  );
}

function ProcessTimeline() {
  return (
    <section className="border-y bg-muted/20 py-24">
      <div className="container-edge">
        <h2 className="text-center font-display text-3xl font-semibold">Our Process</h2>
      </div>
    </section>
  );
}

function SuccessStories() {
  return (
    <section className="container-edge my-24">
      <h2 className="text-center font-display text-3xl font-semibold">Success Stories</h2>
    </section>
  );
}

function UpdatesPreview() {
  return (
    <section className="border-t bg-muted/20 py-24">
      <div className="container-edge">
        <h2 className="font-display text-3xl font-semibold">Latest Updates</h2>
      </div>
    </section>
  );
}

function BlogPreview() {
  return (
    <section className="container-edge my-24">
      <h2 className="font-display text-3xl font-semibold">From Our Blog</h2>
    </section>
  );
}

function FaqPreview() {
  return (
    <section className="border-y bg-muted/20 py-24">
      <div className="container-edge">
        <h2 className="text-center font-display text-3xl font-semibold">FAQ</h2>
      </div>
    </section>
  );
}

function ConsultationCta() {
  return (
    <section className="container-edge my-24">
      <div className="rounded-3xl bg-primary p-12 text-center text-primary-foreground">
        <h2 className="font-display text-3xl font-semibold">Ready to start?</h2>
        <Button asChild size="lg" variant="secondary" className="mt-8">
          <Link href="/contact">Book Consultation</Link>
        </Button>
      </div>
    </section>
  );
}
